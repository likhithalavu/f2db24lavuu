extends layout

block content
  h1= title
  <br>
  p Welcome to Lab10  #{title}
  if (!user) 
    a(href="/login") Login 
    br 
    a(href="/register") Register 
  if (user) 
    p You are currently logged in as #{user.username} 
    a(href="/names") Take me to names 
    br 
    a(href="/logout") Logout 